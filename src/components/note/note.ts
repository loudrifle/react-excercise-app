interface NoteProps {
  title: string;
  description: string;
  data_creation: Date;
  date_updated: Date;
}

export const Note = ({
  title,
  description,
  data_creation,
  date_updated,
}: NoteProps) => {
  return;
};
